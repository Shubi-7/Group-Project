
create Respoitory in GitHub then in ReadME there is this code which you copy and add in Notepad.


echo "# Worldwide" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin git@github.com:rockysil/Worldwide.git
git push -u origin master



then copy this in GitBash of same folder

check git status to see whether they are in repo or not if not in repo red color indicate it

after that

commiting 

git commit -m 'first commit',

now white color indicate it

git push -u origin 'master'

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>.
to create branch

git branch -m 'Bipin' here Bipin is anther branch

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>.
to change branch we write

git checkout -m 'bipin'
git checkout -m 'master'


compare and merge in GitHub....